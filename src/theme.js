import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#f47227",
    },
  },
  typography: {
    fontFamily: "Poppins",
  },
  background: {
    default: "#fff",
  },
});

export default theme;
